{
  "name": "fringematrix-e2e",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/node": "^20.0.0"
  },
  "scripts": {
    "test": "npm --prefix .. run build && playwright test",
    "e2e": "npm --prefix .. run build && playwright test",
    "e2e:headed": "playwright test --headed",
    "e2e:conservative": "npm --prefix .. run build && playwright test --config=playwright.config.test.ts",
    "e2e:report": "playwright show-report"
  }
}



############################
# Node / general
############################
node_modules/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.DS_Store
Thumbs.db

.env
.env.*

# Builds / coverage (not used now, but safe to ignore)
dist/
build/
coverage/
.nyc_output/

############################
# App data
############################
# User-provided images; keep out of git by default
avatars/**

deploy/ip_address.md
build-info.json

server/junit.xml

e2e/playwright-report/
e2e/test-results/

playwright-report/
test-results/
.vercel
.env*.local

# Beads / Dolt files (added by bd init)
.dolt/
.beads/*.db
.beads-credential-key

# Cursor / AI conversation logs — large, not needed in repo
ai_logs/

# Claude Code agent worktrees (created locally, never committed)
.claude/worktrees/

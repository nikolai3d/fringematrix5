import React from 'react';
import ReactDOM from 'react-dom/client';
import GlyphSpinnerDebug from './GlyphSpinnerDebug';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <GlyphSpinnerDebug />
  </React.StrictMode>
);

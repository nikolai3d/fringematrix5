{
  "name": "fringematrix",
  "version": "1.0.0",
  "private": true,
  "description": "Fringe Matrix Gallery - A digital archive for fan-created avatar images",
  "type": "commonjs",
  "main": "server/server.js",
  "scripts": {
    "build": "npm run generate:build-info && npm run build:client",
    "build:client": "cd client && vite build",
    "generate:build-info": "node scripts/generate-build-info.js",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "node server/server.js",
    "dev:client": "cd client && vite",
    "start": "node server/server.js",
    "preview": "cd client && vite preview",
    "test": "npm run test:server && npm run test:client",
    "test:server": "jest --config server/jest.config.js",
    "test:server:watch": "jest --config server/jest.config.js --watch",
    "test:server:ci": "jest --config server/jest.config.js --ci --reporters=default --reporters=jest-junit",
    "test:client": "cd client && vitest run",
    "test:client:ci": "cd client && vitest run",
    "test:e2e": "playwright test --config e2e/playwright.config.ts",
    "test:e2e:headed": "playwright test --config e2e/playwright.config.ts --headed",
    "test:e2e:conservative": "playwright test --config e2e/playwright.config.test.ts",
    "test:e2e:report": "playwright show-report e2e/playwright-report",
    "lint": "echo 'No linter configured'",
    "typecheck": "echo 'No TypeScript configured'"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@vercel/blob": "^0.23.4",
    "express": "^4.19.2",
    "js-yaml": "^4.1.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/node": "^20.0.0",
    "@vitejs/plugin-react": "^4.3.1",
    "concurrently": "^8.2.2",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "supertest": "^7.0.0",
    "vite": "^5.4.0",
    "vitest": "^1.6.0"
  }
}